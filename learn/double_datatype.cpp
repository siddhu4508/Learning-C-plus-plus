#include <iostream>
using namespace std;

int main(){

    double m=1.34000134;
    long double n=1.1231241241445;

    cout<<"size of the double datatype is: "<<sizeof(double)<<" and the element size is: "<< m << endl;
    cout<<"size of the long double datatype is: "<<sizeof(long double)<<" and the element size is: "<< n << endl;

    return 0;
}